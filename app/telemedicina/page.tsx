"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import NewServiceCheckout from "@/components/NewServiceCheckout";

type Slot={clinicianId:string;clinicianName:string;startsAt:string;durationMinutes:number};
export default function TelemedicinePage(){const [source,setSource]=useState("direct");const [slots,setSlots]=useState<Slot[]>([]);const [message,setMessage]=useState("");const [appointmentId,setAppointmentId]=useState("");useEffect(()=>{setSource(new URLSearchParams(window.location.search).get("source")||"direct");fetch("/api/telemedicine").then(r=>r.json()).then(x=>setSlots(x.slots||[])).catch(()=>setSlots([]))},[]);async function reserve(slot:Slot){const r=await fetch("/api/telemedicine",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({clinicianId:slot.clinicianId,startsAt:slot.startsAt,sourceFlow:source})});const b=await r.json();if(r.status===401){setMessage("Ingresa a tu cuenta para reservar este horario.");return}if(!r.ok){setMessage(b.error);return}setAppointmentId(b.id);setMessage("Horario seleccionado. Confirma el pago para reservarlo.")}return <main className="veramed-page min-h-screen px-5 py-12"><div className="mx-auto max-w-2xl"><p className="veramed-kicker">Telemedicina Veramed</p><h1 className="veramed-display mt-3 text-4xl">Evaluación médica · 20 minutos</h1><p className="mt-4 text-slate-600">La consulta puede terminar en una receta, otra alternativa o en la recomendación de no usar medicamentos, según la evaluación.</p><section className="veramed-panel mt-8 p-6"><div className="flex items-end justify-between"><div><span className="text-sm text-slate-500">Precio</span><strong className="block text-2xl">$19.990</strong></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-sm text-emerald-800">20 minutos</span></div>{!appointmentId?<><h2 className="mt-7 font-semibold">Horarios disponibles</h2>{slots.length?<div className="mt-3 grid gap-3">{slots.map(s=><button key={s.clinicianId+s.startsAt} onClick={()=>reserve(s)} className="veramed-secondary-button justify-between px-4 py-3"><span>{new Intl.DateTimeFormat("es-CL",{dateStyle:"medium",timeStyle:"short"}).format(new Date(s.startsAt))}</span><span>{s.clinicianName}</span></button>)}</div>:<p className="mt-3 rounded-2xl bg-slate-50 p-4 text-sm text-slate-600">No hay horarios publicados por el equipo médico en este momento.</p>}</>:<NewServiceCheckout orderId={appointmentId} baseAmount={19990} requestType="telemedicine" onError={setMessage}/>} {message&&<p className="mt-4 text-sm font-medium">{message} {message.startsWith("Ingresa")&&<Link href="/ingresar" className="text-emerald-700 underline">Ingresar</Link>}</p>}</section></div></main>}
